import React from 'react'
import IndexLayout from '../layouts'

const ScreencastTemplate = ({ data }: any) => {
    console.log(data)

    return (
        <IndexLayout>
            Screencast.
        </IndexLayout>
    )
}

export default ScreencastTemplate
